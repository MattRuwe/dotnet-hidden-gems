<Window x:Class="WPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800" Loaded="MainWindow_OnLoaded">
    <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <TextBlock x:Name="tbText" FontSize="18pt" />
        <Button x:Name="BtnClick" FontSize="24" Margin="10" Padding="10" Click="BtnClick_OnClick">
            <TextBlock>UI Thread</TextBlock>
        </Button>
        <Button x:Name="BtnDeadlock" FontSize="24" Margin="10" Padding="10" Click="BtnDeadlock_OnClick">
            <TextBlock>Deadlock</TextBlock>
        </Button>
    </StackPanel>
</Window>
